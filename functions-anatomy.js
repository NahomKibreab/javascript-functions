/**
// Declaration function
  function name(param0) {
     statements;
     return value;
  }
 */

/**
 * // function expression
 * const name = function(param0) {
 *  statements;
 *  return value;
 * }
 */

/**
 * // Arrow Expression function
 * const fun = () => expression // Example 1
 * const fun2 = (param1, paramN) => expression // Example 2
 * const fun3 = (param1, paramN) => {
                  statements
                  return value;
                } // Example 3
 */

/**
 * // Anonymous function
 * (function () {
      console.log("FOO!");
    }
    )();
 */
